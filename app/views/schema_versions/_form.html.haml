= form_tag [@schema, schema_version], multipart: true do
  - if schema_version.errors.any?
    #error_explanation
      %h2
        = pluralize(schema_version.errors.count, "error")
        prohibited this schema_version from being saved:
      %ul
        - schema_version.errors.full_messages.each do |message|
          %li= message
  %label{:for => "spec_file"} Spec to Upload
  = file_field_tag "spec_file"
  .field
  = submit_tag("Crear")
