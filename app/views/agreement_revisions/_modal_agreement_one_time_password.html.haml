#modalAgreementOneTimePassword.modal.fade{:role => "dialog", :tabindex => "-1"}
  .modal-dialog
    .modal-content
      .modal-header
        %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
          %span{"aria-hidden" => "true"} ×
        %h1=t(:one_time_password)
      .modal-body
        %h3
          = text_field_tag :one_time_password
        .modal-footer
          %button.btn.btn-default{"data-dismiss" => "modal", :type => "button"}
            =t(:cancel)
          = content_tag(:a, t(:sign_request), class: 'btn btn-success', href: one_time_password_path(:one_time_password))