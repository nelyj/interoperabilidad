#modalAgreementObjected.modal.fade{:role => "dialog", :tabindex => "-1"}
  .modal-dialog
    .modal-content
      .modal-header
        %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
          %span{"aria-hidden" => "true"} ×
        %h1=t(:object_agreement)
      .modal-body
        = form_for @agreement, url: organization_agreement_change_state_path(@organization, @agreement, @agreement.next_step) do |f|
          %h3
            =t(:rejection_reason)
          = f.text_area :objection_message
          .modal-footer
            %button.btn.btn-default{"data-dismiss" => "modal", :type => "button"}
              =t(:cancel)
            %button.btn.btn-danger{:type => "submit"}
              =t(:reject)